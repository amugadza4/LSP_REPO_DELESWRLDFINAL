# AI Usage Report – Question 2

## AI Tools Used:
ChatGPT (OpenAI)

## Prompts Used:

1. "Can you explain the Template Method design pattern and how to 
implement it in Java with an abstract class?"

2. "How do I use the final keyword on a method in an abstract class to 
prevent subclasses from overriding the template?"

3. "Can you show me how to demonstrate polymorphism using a List of the 
abstract type and calling the template method in a loop?"

## How AI Helped:
The AI clarified the role of the `final` modifier on the template method 
to lock the workflow while keeping individual steps abstract. It also 
helped confirm the correct structure of the abstract class and how the 
Driver should use `List<Report>` to demonstrate polymorphism. The 
explanations were used to guide the implementation, which I then wrote and 
adapted myself.

## Reflection:
I gained a clearer understanding of how the Template Method pattern 
separates the algorithm's skeleton from its specific steps, making it easy 
to add new report types without changing the shared workflow.
